'use strict';

/**
 * photo-editing-type service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::photo-editing-type.photo-editing-type');
