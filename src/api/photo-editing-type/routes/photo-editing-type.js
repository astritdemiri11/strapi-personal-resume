'use strict';

/**
 * photo-editing-type router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::photo-editing-type.photo-editing-type');
